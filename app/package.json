{
  "name": "mongodb-exporter",
  "version": "1.0.0",
  "description": "Exportador de métricas de MongoDB para Prometheus",
  "main": "exporter.js",
  "scripts": {
    "start": "node exporter.js",
    "docker:build": "docker build -t $npm_config_username/mongodb-profiler-exporter:$npm_config_tag .",
    "docker:push": "docker push $npm_config_username/mongodb-profiler-exporter:$npm_config_tag",
    "docker:publish": "npm run docker:build --username=$npm_config_username --tag=$npm_config_tag && npm run docker:push --username=$npm_config_username --tag=$npm_config_tag",
    "docker:publish:latest": "npm run docker:publish --username=$npm_config_username --tag=latest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.1.0",
    "prom-client": "^14.2.0",
    "dotenv": "^10.0.0"
  },
  "author": "Iván Millán",
  "license": "MIT"
}